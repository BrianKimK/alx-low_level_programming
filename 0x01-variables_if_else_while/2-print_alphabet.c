#include <stdio.h>

/**
 * main - lowercase printing using code
 *
 * Return: 0
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= b'z'; letter++)
		putchar(letter);
	putchar('\n');

	return (0);
}
